import styled from "styled-components";

export const StyledWatch = styled.div`
  .detail-banner {
    object-fit: cover;
    border-radius: 14px;
  }
  .detail-categories,
  .detail-areas {
    display: flex;
    gap: 14px;
  }
`;
